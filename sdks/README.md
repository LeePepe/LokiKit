# SDKs

Client libraries that push logs to the Loki stack in `../stack/`.

- `swift/` — LokiKit, a Swift Package for iOS/macOS. Available today.
- `web/` — Web/TypeScript SDK, added by a sibling task.
